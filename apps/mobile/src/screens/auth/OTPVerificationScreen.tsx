import React from 'react';

import { LayoutScreen } from '@/components/LayoutScreen';

export const OTPVerificationScreen = (): React.JSX.Element => {
  return <LayoutScreen title="Legacy OTP" subtitle="OTP flow is deprecated and no longer used." />;
};
